{
  "buildCommand": "cd connections && npm install && npm run build",
  "outputDirectory": "connections/dist",
  "installCommand": "cd connections && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
